"""One-off model experiments run via the CLI experiment commands.

These modules are not part of the production serving or training path. Each is
driven by a `cli/*_experiment.py` shell, writes raw artifacts to `reports/`,
and is written up in `docs/internal/experiments/`.
"""
